# Iterm2-ssh-tools
> Iterm2 ssh client tools for Iterm2, Linux Terminal, Mac Terminal…etc
## fork by
```bash
git clone git@github.com:lonecloud/ssh-tools.git
```
## config data/ssh.txt
```

1. 机器类型范围值：0 直接连接机器，1. 跳板机  2.目标机
2. ssh中的用户名以及IP(设置端口，非必填)、密码、自定义名称
3. eg:0 username@ip [:port] password Tag

```
